# FizzBuzz

## Approach

The implementation of this exercice was made in TDD way, implementing firstly the functionnal and after extending the algorithm to make it scallable.

## Run FizzBuzz

To fastly display the fizzbuzz in terminal:

- `npm start`

## Run tests

- `npm install`
- `npm test`
